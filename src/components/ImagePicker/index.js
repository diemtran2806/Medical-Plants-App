import {
  StyleSheet,
  View,
  TouchableOpacity,
  Text,
  Image,
  ScrollView,
  TextInput,
} from 'react-native';

const ImagePicker = () => {
  return <></>;
};

export default ImagePicker;

const styles = StyleSheet.create({});
